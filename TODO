- Fix BT algorithm
- Filter input data to avoid errors if invalid characters are fourd (letters, symbols, ...)
- Check if all the generated sudokus have a solution (they should). If not, fix it so just completely valid sudokus are generated.
- Improve comments.